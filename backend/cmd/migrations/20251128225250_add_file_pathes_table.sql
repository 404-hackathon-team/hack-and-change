-- +goose Up
CREATE TABLE IF NOT EXISTS file_pathes (
	"id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
	"name" TEXT NOT NULL,
	"path" TEXT NOT NULL,
	"fileType" VARCHAR(255) NOT NULL,
	PRIMARY KEY("id")
);

-- +goose Down
DROP TABLE IF EXISTS file_pathes;
