-- +goose Up
CREATE TABLE IF NOT EXISTS steps (
   "id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
   "usersAt" INTEGER ARRAY,
   "blocks" INTEGER ARRAY,
   PRIMARY KEY("id")
);


-- +goose Down
DROP TABLE IF EXISTS steps;
