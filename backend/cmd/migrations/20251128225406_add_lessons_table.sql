-- +goose Up
CREATE TABLE IF NOT EXISTS lessons (
	"id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
	"teacher" INTEGER NOT NULL,
	"text" TEXT,
	"blocks" INTEGER ARRAY,
	"createdAt" TIMESTAMP NOT NULL,
	PRIMARY KEY("id")
);

-- +goose Down
DROP TABLE IF EXISTS lessons;
